# Add this line to pyproject.toml under [tool.pytest.ini_options] markers:
#
#   "security: marks security regression tests (one test per audit finding)",
#
# Then the workflow's `-m security` filter selects exactly these tests.
