{
  "mcpServers": {
    "acgs-governance": {
      "command": "python3",
      "args": ["-m", "enhanced_agent_bus.mcp_server.cli", "--transport", "stdio"],
      "env": {
        "PYTHONPATH": "packages:src"
      },
      "description": "ACGS Constitutional Governance — validate_constitutional_compliance, get_active_principles, query_governance_precedents, submit_governance_request, get_governance_metrics. Requires Agent Bus (port 8000) running: pm2 start ecosystem.config.cjs --only agent-bus-8000"
    }
  }
}
