# arckit.acgs

Scan the current arc-kit project for ARC-*-PRIN, ARC-*-RISK, ARC-*-DPIA, and ARC-*-REQ artifacts.

Run:

```bash
acgs arckit generate --from . --out constitution.yaml
```

Then show the generated rule count, severity distribution, constitutional hash, and source artifact IDs.
