[workspace]
members = ["core", "pyo3", "wasm"]
default-members = ["core", "pyo3", "wasm"]
exclude = ["spacetime_governance"]
resolver = "2"

[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
panic = "abort"
