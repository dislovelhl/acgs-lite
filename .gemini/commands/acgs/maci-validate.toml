name = "maci-validate"
description = "Validate role separation and non-self-approval constraints for a workflow or design."
prompt = """
Check the described workflow for MACI compliance.
Explicitly verify:
- proposer, validator, and executor are distinct roles
- no actor validates its own output
- approval and execution paths are independently auditable
Return a short compliance report with violations, severity, and remediation steps.
"""
